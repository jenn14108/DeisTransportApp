extends layout

block content
  .section-only
    .container
      .row
        .col-md-12.text-center
          h2.section-title Log In
          p.section-description You must have a Brandeis account to log in
        br
        .col-md-12.text-center
          a.btn.btn-danger(href="/auth/google")
              span.fa.fa-google-plus  Sign In
